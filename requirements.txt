# =============================================================================
# Spatial Data Science: The New Frontier in Analytics
# Python 3.11 or 3.12 recommended (3.13 works; 3.14 wheels are still catching up)
#
#   python3.12 -m venv gis-env
#   source gis-env/bin/activate      # Windows: gis-env\Scripts\activate
#   pip install -r requirements.txt
# =============================================================================

# ---- core scientific stack --------------------------------------------------
numpy>=1.26
pandas>=2.1
scipy>=1.11

# ---- the three pillars of the course ---------------------------------------
geopandas>=1.0          # spatial data manipulation - the spine of the course
shapely>=2.0            # geometry engine (GEOS); 2.x is vectorised and required
rasterio>=1.3           # raster I/O and processing (GDAL)

# ---- vector I/O engines -----------------------------------------------------
pyogrio>=0.8            # GeoPandas 1.x default; 5-20x faster than Fiona
fiona>=1.9              # streaming alternative, still widely used
pyproj>=3.6             # CRS handling and geodesic calculations (PROJ)

# ---- visualisation ----------------------------------------------------------
matplotlib>=3.8
seaborn>=0.13
mapclassify>=2.6        # choropleth classification (quantiles, natural breaks)
contextily>=1.5         # OPTIONAL: web basemap tiles; needs internet

# ---- statistics and machine learning ---------------------------------------
scikit-learn>=1.4       # clustering and the predictive models in Module 3
statsmodels>=0.14       # regression INFERENCE: std errors, t, p, AIC (Lesson A9)

# ---- fast intermediate storage ---------------------------------------------
pyarrow>=15.0           # OPTIONAL: GeoParquet read/write

# ---- notebook ---------------------------------------------------------------
jupyterlab>=4.0
ipykernel>=6.29

# ---- OPTIONAL, only needed to REBUILD the PDF from course_src/ -------------
# markdown>=3.5
# pygments>=2.17
# weasyprint>=60
